spring:
  application:
    name: "fundamentos01"
  
  datasource:
    url: jdbc:postgresql://localhost:5432/db_portafolio
    username: postgres 
    password: ups123
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true 
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080